name: release

# 正式发布只由 tag 触发（稳定版由人工打 tag 拍板）；workflow_dispatch 为
# 手动补发已有 tag 的入口。PR/普通 push 不产生任何发布物（只跑 ci.yml）。
# 打包用 git archive：.gitattributes 的 export-ignore 自动剔除开发资产
# （tests/、.github/）。
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "要发布的已有 tag（如 v1.31.0）"
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      # 与 ci.yml 测试 job 同布局（子目录检出，tests 以包名导入）
      - uses: actions/checkout@v4
        with:
          path: astrbot_plugin_xinxian
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: 安装依赖
        run: pip install pytest
      - name: 测试门禁（不过不出包）
        run: python -m pytest astrbot_plugin_xinxian/tests/ -q
      - name: 构建发布包
        run: |
          TAG="${{ inputs.tag || github.ref_name }}"
          cd astrbot_plugin_xinxian
          git archive --format=zip -o "../astrbot_plugin_xinxian-${TAG}.zip" "refs/tags/${TAG}"
          cd ..
          echo "===== 产物清单（应无 tests/.github） ====="
          unzip -l "astrbot_plugin_xinxian-${TAG}.zip" | awk '{print $4}' | grep -E '^(tests/|\.github/)' && {
            echo "⛔ 开发资产混入发布包"; exit 1; } || true
          unzip -l "astrbot_plugin_xinxian-${TAG}.zip" | tail -3
      - name: 发布 GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ inputs.tag || github.ref_name }}"
          cd astrbot_plugin_xinxian
          PREV=$(git describe --tags --abbrev=0 "refs/tags/${TAG}^" 2>/dev/null || true)
          NOTES=$(git log --oneline ${PREV:+$PREV..}"refs/tags/${TAG}" | head -80 || git log --oneline -40)
          cd ..
          PRE_ARGS=""
          case "$TAG" in *-*) PRE_ARGS="--prerelease" ;; esac
          gh release create "$TAG" "astrbot_plugin_xinxian-${TAG}.zip" \
            --title "$TAG" --notes "$NOTES" $PRE_ARGS
